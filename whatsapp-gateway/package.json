{
  "name": "whatsapp-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "Yassen Mohamed Kotb | 01288536381 — local WhatsApp Web gateway for QR card delivery (binds 127.0.0.1 only). Requires the wppconnect engine pre-installed in the GymSystem data dir (Settings → install engine) — this package has NO npm dependencies and ships with zero node_modules.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "install-deps": "node standalone-bin.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}